// 액션 정의 — 클라가 서버로 보낼 수 있는 것의 전부.
// 이 목록이 그대로 나중의 API 스펙이 된다.
//   예: buyGenerator / buyUpgrade / buyFameShop / prestige
//       tapBatch(count, from, to) ← 탭은 개별 전송 금지, 반드시 배치
//       claimGolden / claimDaily / claimOffline
export {};
